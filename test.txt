import { CommonType, COMMON_CONSTANT } from './index';

describe('ServiceNow Common Module', () => {
  it('should define CommonType with the correct field type', () => {
    const testValue: CommonType = { field: 'testString' };
    expect(typeof testValue.field).toBe('string');
  });

  it('should export COMMON_CONSTANT with the correct value', () => {
    expect(COMMON_CONSTANT).toBe(1);
  });

  it('should throw a type error if field in CommonType is not a string', () => {
    // This is more of a TypeScript type-check test rather than runtime
    // Uncommenting this will cause a TypeScript error during compilation.
    // const invalidValue: CommonType = { field: 123 }; // Should throw a TS error
    expect(true).toBe(true); // Dummy assertion for runtime testing
  });
});