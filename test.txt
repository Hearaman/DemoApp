components:
  schemas:
    ApplicationInfo:
      type: object
      properties:
        u_used_for:
          type: string
          description: The environment the application is used for (e.g., Production, Development).
        name:
          type: string
          description: The name of the application.
        u_application_id:
          type: string
          description: The unique application ID.
      required:
        - u_used_for
        - name
        - u_application_id

    ApplicationsResponse:
      type: object
      properties:
        result:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationInfo'
      required:
        - result